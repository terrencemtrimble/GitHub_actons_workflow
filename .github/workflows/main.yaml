name: My GitHub Actions Workflow
on: [push]
jobs: 
  testing_github: 
    runs-on: ubuntu-latest
    steps:
      - name: Hello   
        run: echo "Hello, GitHub Actions!"
        name: Display repo name 
        run: echo "the workflow is running in the repository: ${{ github.repository }}"